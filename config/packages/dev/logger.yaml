parameters:
    handlers:
        StreamHandler:
            host: "php://stdout"
            level: 100
#        GelfHandlerTcp:
#            host: "127.0.0.1" # default host for TcpTransport
#            port: 12201 # default port for TcpTransport
#            source: "GelfHandlerTcp" #example
#            level: 100
    app_name: "common-service-template-api"
